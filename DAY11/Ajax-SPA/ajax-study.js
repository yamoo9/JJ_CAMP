/*! ajax-study.js © yamoo9.net, 2016 */
(function(global, XHR){
  'use strict';

  // Ajax (비동기 통신)를 수행할 객체 생성
  var xhr = new XHR();
  // 생성된 XHR 객체 검토
  console.dir(xhr);

})(this, this.XMLHttpRequest);